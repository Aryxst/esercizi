#include <math.h>
#include <stdio.h>

int main() {
	double x, a, b, c, delta, x1, x2;
	delta = pow(b, 2) - (4 * a * c);
	if (delta >= 0) {
		printf("Ci sono due possibili risultati");
	}
	return 0;
}
